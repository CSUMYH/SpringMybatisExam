<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.mao.hand.Beans.Address"> <!-- mapper 中的 namespace 指明映射的bean是哪一个 -->
	
<!-- 	// resultMap 标签下是配置bean中的属性与数据库表中的列对应 -->
	<resultMap type="Address" id="AddressResult">	<!-- //type 是指自定义的Bean类型，id是指对这个映射的标识 -->
	    <id	property="address_id" column="address_id"/>	       <!-- //ID 子标签对应的是 表的id-->
		<result property="address" column="address"/> 
		<result property="address2" column="address2"/>
		<result property="district" column="district"/>
		<result property="city_id" column="city_id"/>
		<result property="postal_code" column="postal_code"/>
		<result property="phone" column="phone"/>
		<result property="last_update" column="last_update"/>
	</resultMap>
  	
  	<!-- 通过<select>标签写查询语句，如果返回多个结果，就使用resultMap,返回一个结果，就使用resultType -->
  	<select id ="findAllAddress" resultMap ="AddressResult">
  		select * from address
  	</select>
</mapper>